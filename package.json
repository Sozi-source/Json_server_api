{
  "name": "my-json-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}